{
  "name": "neon-arcade-worker",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute neon-arcade-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute neon-arcade-db --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "wrangler": "^3.99.0",
    "typescript": "^5.6.3"
  }
}
